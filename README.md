# DevOps nginx Demo

## Intro

This project help you install nginx and show demo page.

## Steps

sudo apt-get update
sudo apt install nginx
sudo systemctl start nginx
sudo systemctl enable nginx

webpage is kept at /var/www/nginx
